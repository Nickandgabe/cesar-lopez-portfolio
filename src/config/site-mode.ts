/**
 * Toggle to show a lightweight "in progress" landing page instead of the
 * full site, while real project content (images/video/3D models) is being
 * gathered. Flip to false and redeploy once real content has replaced the
 * placeholders in src/content/projects.ts.
 */
export const MAINTENANCE_MODE = true;
